-r requirements.txt

# Только для panel.py (веб-панель). Боту не нужны.
fastapi>=0.115
uvicorn>=0.32
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.2
starlette>=0.41

# Только для PostgreSQL вместо SQLite.
asyncpg>=0.30
